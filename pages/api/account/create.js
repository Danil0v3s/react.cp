export default async(req, res) => {
    const {
        serverGroupName,
        username,
        password,
        passwordConfirm,
        email,
        emailConfirm,
        gender,
        birthDate,
        code
    } = req.body;
}